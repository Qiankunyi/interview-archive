# Interview Archive

This repository deploys the submitted Spring Boot WAR without changing its
Thymeleaf templates, CSS, fonts, Java logic, or bundled seed data.

The application starts with `java -jar interview-archive.war` and restores the
28 bundled interview records on every server start, matching the submitted
local build.
